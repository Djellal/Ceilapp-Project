@page "/edit-compensation"
@attribute [Authorize]

<PageTitle>Modifier la séance de rattrapage</PageTitle>
<div class="container-fluid py-4">
    <RadzenCard class="shadow-lg rounded-4 border-0">
        <RadzenCardHeader class="bg-primary bg-opacity-10 border-bottom">
            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="1rem">
                <RadzenIcon Icon="edit" Style="font-size:1.75rem" />
                <RadzenText TextStyle="TextStyle.H5" class="mb-0 fw-bold">Modifier la séance de rattrapage</RadzenText>
            </RadzenStack>
        </RadzenCardHeader>
        <div class="p-4">
            <RadzenAlert Shade="Shade.Lighter" Variant="Variant.Flat" Size="AlertSize.Small" AlertStyle="AlertStyle.Danger" Visible="@errorVisible">@errorMessage</RadzenAlert>
            <RadzenTemplateForm TItem="Ceilapp.Models.ceilapp.Compensation" Data="@(compensation)" Visible="@(compensation != null)" Submit="@FormSubmit">
                <RadzenStack Gap="1rem">
                    <RadzenFormField Text="Inscription de l'étudiant" Variant="Variant.Filled">
                        <ChildContent>
                            <RadzenDropDown TValue="int" Data="@(registrationDisplayItems)" TextProperty="DisplayText" ValueProperty="Id"
                                AllowClear="true" AllowFiltering="true" FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive"
                                style="display: block; width: 100%" @bind-Value="@(compensation.CourseRegistrationId)" Name="CourseRegistrationId"
                                Placeholder="Rechercher un étudiant par nom, code ou cours..." />
                        </ChildContent>
                        <Helper>
                            <RadzenRequiredValidator Component="CourseRegistrationId" Text="L'inscription de l'étudiant est requise" DefaultValue="0" />
                        </Helper>
                    </RadzenFormField>

                    <RadzenText TextStyle="TextStyle.Subtitle1" class="fw-bold mt-3">
                        <i class="bi bi-calendar-x me-2"></i>Détails de l'absence
                    </RadzenText>

                    <RadzenFormField Text="Date d'absence" Variant="Variant.Filled">
                        <ChildContent>
                            <RadzenDatePicker DateFormat="dd/MM/yyyy" style="display: block; width: 100%" @bind-Value="@(compensation.AbsenceDate)" Name="AbsenceDate" />
                        </ChildContent>
                        <Helper>
                            <RadzenRequiredValidator Component="AbsenceDate" Text="La date d'absence est requise" />
                        </Helper>
                    </RadzenFormField>

                    <RadzenStack Orientation="Orientation.Horizontal" Gap="1rem">
                        <RadzenFormField Text="Heure début absence" Variant="Variant.Filled" style="flex:1">
                            <ChildContent>
                                <RadzenMask CharacterPattern="[0-9]" Mask="**:**:**" Value="@(compensation.AbsenceFrom.ToString())" Change="@(args => { try { compensation.AbsenceFrom = TimeSpan.Parse(!string.IsNullOrEmpty(args) ? args : "00:00"); } catch{} })" style="display: block; width: 100%" Name="AbsenceFrom" />
                            </ChildContent>
                            <Helper>
                                <RadzenRequiredValidator Component="AbsenceFrom" Text="L'heure de début d'absence est requise" />
                            </Helper>
                        </RadzenFormField>

                        <RadzenFormField Text="Heure fin absence" Variant="Variant.Filled" style="flex:1">
                            <ChildContent>
                                <RadzenMask CharacterPattern="[0-9]" Mask="**:**:**" Value="@(compensation.AbsenceTo.ToString())" Change="@(args => { try { compensation.AbsenceTo = TimeSpan.Parse(!string.IsNullOrEmpty(args) ? args : "00:00"); } catch{} })" style="display: block; width: 100%" Name="AbsenceTo" />
                            </ChildContent>
                            <Helper>
                                <RadzenRequiredValidator Component="AbsenceTo" Text="L'heure de fin d'absence est requise" />
                            </Helper>
                        </RadzenFormField>

                        <RadzenStack Orientation="Orientation.Vertical" Gap="0.25rem" Style="align-self: flex-end;">
                            <RadzenButton Size="ButtonSize.Small" Variant="Variant.Outlined" Text="9:00-12:00" Click="@(() => SetAbsenceTimeSlot(9, 12))" Title="Sélectionner 9:00 - 12:00" />
                            <RadzenButton Size="ButtonSize.Small" Variant="Variant.Outlined" Text="13:00-16:00" Click="@(() => SetAbsenceTimeSlot(13, 16))" Title="Sélectionner 13:00 - 16:00" />
                        </RadzenStack>
                    </RadzenStack>

                    <RadzenText TextStyle="TextStyle.Subtitle1" class="fw-bold mt-3">
                        <i class="bi bi-calendar-check me-2"></i>Séance de rattrapage
                    </RadzenText>

                    <RadzenFormField Text="Date de rattrapage" Variant="Variant.Filled">
                        <ChildContent>
                            <RadzenDatePicker DateFormat="dd/MM/yyyy" style="display: block; width: 100%" @bind-Value="@(compensation.MakeupDate)" Name="MakeupDate" />
                        </ChildContent>
                        <Helper>
                            <RadzenRequiredValidator Component="MakeupDate" Text="La date de rattrapage est requise" />
                        </Helper>
                    </RadzenFormField>

                    <RadzenStack Orientation="Orientation.Horizontal" Gap="1rem">
                        <RadzenFormField Text="Heure début rattrapage" Variant="Variant.Filled" style="flex:1">
                            <ChildContent>
                                <RadzenMask CharacterPattern="[0-9]" Mask="**:**:**" Value="@(compensation.MakeupFrom.ToString())" Change="@(args => { try { compensation.MakeupFrom = TimeSpan.Parse(!string.IsNullOrEmpty(args) ? args : "00:00"); } catch{} })" style="display: block; width: 100%" Name="MakeupFrom" />
                            </ChildContent>
                            <Helper>
                                <RadzenRequiredValidator Component="MakeupFrom" Text="L'heure de début de rattrapage est requise" />
                            </Helper>
                        </RadzenFormField>

                        <RadzenFormField Text="Heure fin rattrapage" Variant="Variant.Filled" style="flex:1">
                            <ChildContent>
                                <RadzenMask CharacterPattern="[0-9]" Mask="**:**:**" Value="@(compensation.MakeupTo.ToString())" Change="@(args => { try { compensation.MakeupTo = TimeSpan.Parse(!string.IsNullOrEmpty(args) ? args : "00:00"); } catch{} })" style="display: block; width: 100%" Name="MakeupTo" />
                            </ChildContent>
                            <Helper>
                                <RadzenRequiredValidator Component="MakeupTo" Text="L'heure de fin de rattrapage est requise" />
                            </Helper>
                        </RadzenFormField>

                        <RadzenStack Orientation="Orientation.Vertical" Gap="0.25rem" Style="align-self: flex-end;">
                            <RadzenButton Size="ButtonSize.Small" Variant="Variant.Outlined" Text="9:00-12:00" Click="@(() => SetMakeupTimeSlot(9, 12))" Title="Sélectionner 9:00 - 12:00" />
                            <RadzenButton Size="ButtonSize.Small" Variant="Variant.Outlined" Text="13:00-16:00" Click="@(() => SetMakeupTimeSlot(13, 16))" Title="Sélectionner 13:00 - 16:00" />
                        </RadzenStack>
                    </RadzenStack>

                    <RadzenFormField Text="Enseignant de rattrapage" Variant="Variant.Filled">
                        <ChildContent>
                            <RadzenAutoComplete Data="@teacherNames" style="display: block; width: 100%"
                                @bind-Value="@(compensation.MakeupTeacherId)" Name="MakeupTeacherId"
                                Placeholder="Saisir le nom de l'enseignant..." FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive" />
                        </ChildContent>
                        <Helper>
                            <RadzenRequiredValidator Component="MakeupTeacherId" Text="L'enseignant de rattrapage est requis" />
                        </Helper>
                    </RadzenFormField>

                    <RadzenText TextStyle="TextStyle.Subtitle1" class="fw-bold mt-3">
                        <i class="bi bi-people me-2"></i>Informations complémentaires
                    </RadzenText>

                    <RadzenFormField Text="Niveau du cours" Variant="Variant.Filled">
                        <ChildContent>
                            <RadzenAutoComplete Data="@courseLevelSuggestions" style="display: block; width: 100%"
                                @bind-Value="@(compensation.CourseLevel)" Name="CourseLevel"
                                Placeholder="Saisir le niveau du cours..." FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive" />
                        </ChildContent>
                    </RadzenFormField>

                    <RadzenStack Orientation="Orientation.Horizontal" Gap="1rem">
                        <RadzenFormField Text="Groupe d'origine" Variant="Variant.Filled" style="flex:1">
                            <ChildContent>
                                <RadzenAutoComplete Data="@originGroupSuggestions" style="display: block; width: 100%"
                                    @bind-Value="@(compensation.OriginGroup)" Name="OriginGroup"
                                    Placeholder="Saisir le groupe d'origine..." FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive" />
                            </ChildContent>
                        </RadzenFormField>

                        <RadzenFormField Text="Groupe destinataire" Variant="Variant.Filled" style="flex:1">
                            <ChildContent>
                                <RadzenAutoComplete Data="@recipientGroupSuggestions" style="display: block; width: 100%"
                                    @bind-Value="@(compensation.RecipientGroup)" Name="RecipientGroup"
                                    Placeholder="Saisir le groupe destinataire..." FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive" />
                            </ChildContent>
                        </RadzenFormField>
                    </RadzenStack>

                    @if (Security.IsInRole(Constants.ADMIN))
                    {
                        <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="0.75rem" class="mt-2 p-3 rounded-3" Style="background-color: var(--rz-base-200);">
                            <RadzenCheckBox @bind-Value="@(compensation.IsApproved)" Name="IsApproved" />
                            <RadzenLabel Text="Approuver cette séance de rattrapage" Component="IsApproved" class="fw-semibold" />
                        </RadzenStack>
                    }
                </RadzenStack>

                <RadzenStack style="margin-top:1.5rem;" Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" JustifyContent="JustifyContent.End" Gap="0.5rem">
                    <RadzenButton ButtonStyle="ButtonStyle.Primary" ButtonType="ButtonType.Submit" Icon="save" Text="Enregistrer" Variant="Variant.Flat" />
                    <RadzenButton ButtonStyle="ButtonStyle.Light" Text="Annuler" Variant="Variant.Flat" Click="@CancelButtonClick" />
                </RadzenStack>
            </RadzenTemplateForm>
        </div>
    </RadzenCard>
</div>
